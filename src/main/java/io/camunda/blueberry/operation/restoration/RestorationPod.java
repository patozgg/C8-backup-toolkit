package io.camunda.blueberry.operation.restoration;

public class RestorationPod {

    public void createPodRestoration() {
        /*
          try (KubernetesClient client = new KubernetesClientBuilder().build()) {
            File yamlFile = new File("myFile.yaml"); // Path to your YAML file
            client.load(yamlFile).createOrReplace();
            System.out.println("Pod created successfully!");
         */
    }
}
