package io.camunda.blueberry.connect.toolbox;

public class KubenetesToolbox {

    /**
     * Check if a pod exist in the cluster.
     *
     * @param podName pod name, like "tasklist"
     * @return true is one pod is detected
     */
    public boolean isPodExist(final String podName) {
        return true;
    }
}
